// Generated by CoffeeScript 1.6.3
(function() {
  var libr;

  libr = angular.module('starter.controllers', []);

  libr.controller('MainCtrl', [
    '$scope', '$location', function($scope, $location) {
      return $scope.goTo = function(page) {
        $scope.sideMenuController.toggleRight();
        return $location.url('/' + page);
      };
    }
  ]).controller('HomeCtrl', function($scope, BookService) {
    return BookService.getBooks().success(function(result) {
      $scope.books = result.books;
      $scope.enableBackButton = false;
      $scope.rightButtons = [
        {
          type: 'button-icon icon ion-navicon',
          tap: function(e) {
            return $scope.sideMenuController.toggleRight();
          }
        }
      ];
      return $scope.leftButtons = [];
    });
  }).controller('PetDetailCtrl', function($scope, $stateParams, BookService) {
    return BookService.getBook($stateParams.isbn).success(function(result) {
      $scope.book = result;
      $scope.enableBackButton = true;
      $scope.rightButtons = [
        {
          type: 'button-icon icon ion-navicon',
          tap: function(e) {
            return $scope.sideMenuController.toggleRight();
          }
        }
      ];
      $scope.leftButtons = [];
      return $scope.bookName = result.name;
    });
  });

}).call(this);
